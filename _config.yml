title: COZ GIS Wiki

description: A Jekyll Theme
baseurl: "/wiki"
url: "https://zanesville.github.io"

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "page"
      
plugins:
  - jekyll-admin
  
highlighter: rouge
